import { type Module } from '../schema';

export async function getModules(courseId: number): Promise<Module[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all modules for a specific course.
    // Should return modules ordered by order_index for proper course structure.
    return [];
}